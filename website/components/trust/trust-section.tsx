const gates = ["Final elemental magnesium and percentage RDA", "Complete Supplement Facts and required declarations", "Tamper-evident packaging construction", "Batch-specific testing documentation when available"];
export function TrustSection() { return <section className="trust section" aria-labelledby="trust-title"><div className="wrap"><p className="eyebrow">TRUST BEFORE TESTIMONIALS</p><h2 id="trust-title" className="display">What we will publish before orders open.</h2><div className="trust__list">{gates.map((gate, index) => <article key={gate}><span className="data">0{index + 1}</span><p>{gate}</p><span aria-hidden="true">↗</span></article>)}</div></div></section>; }
